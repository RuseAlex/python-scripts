# xml2json
The scripts takes an xml document and converts into a json format while still keeping the original copy.

# How to use it
```bash
python3 xml2json -f doc.xml 
```

# Dependencies
* mltodict